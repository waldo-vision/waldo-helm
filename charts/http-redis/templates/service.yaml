apiVersion: v1
kind: Service
metadata:
  name: {{ include "http-redis.fullname" . }}
  labels:
    {{- include "http-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "http-redis.selectorLabels" . | nindent 4 }}
